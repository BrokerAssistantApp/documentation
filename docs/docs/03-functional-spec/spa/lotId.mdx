---
sidebar_position: 5
title: Лот
description: Просмотр и редактирование информации о лоте - статус, участники, инвестиции, расходы, выплаты
---

# Лот

## 🎯 Цель

Предоставляет брокеру подробную информацию о лоте, включая статус, участников, инвестиции, расходы, продажи. Позволяет редактировать данные, управлять участниками и транзакциями.

---

## 🔗 URL

- Страница доступна по `/dashboard/lots/:lotId`


---

## 💻 Макет в Figma

<iframe
  style={{ border: '1px solid #ccc', borderRadius: '8px' }}
  width="100%"
  height="500"
  src="https://embed.figma.com/design/bO4l0BqzjfCQHvWnIakQuM/BrokerAssistant?embed-host=share"
  allowFullScreen
/>
---

## 🛠️ Функциональность

### 💻 Frontend

#### 🔹 Основной блок

- Название лота
- Статус лота
- Дата покупки и цена
- Себестоимость
- Дата и цена продажи
- Комиссия и налог брокера (в ₽ и %)
- Ссылки: объявления (Авито, Циан), ЕФРС, ЭТП

#### 🔹 Блок участников

- Список участников:
  - Имя
  - Общая сумма инвестиций
  - Сумма, участвующая в расчёте долей
  - Доля (%) или сумма (₽)
  - Реферальная связь, выплата и кто платит
  - Кнопка «Добавить участника»
  - Форма добавления:
    - Участник
    - Доля (в % или ₽)
    - Инвестиции (суммы, чекбоксы «учитывать в расчёте»)
    - Реферер, бонус, кто платит

#### 🔹 Блок расходов

- Список расходов:
  - Название
  - Сумма
  - Учтён в себестоимости или нет
  - Кнопка «Добавить расход»
  - Форма:
    - Описание
    - Сумма
    - Чекбокс «учесть в себестоимости»

#### 🔹 История изменения цены продажи

- В заголовке блока **"Продажа"** отображается текущая `salePrice`
- При клике на заголовок или иконку рядом открывается **модальное окно с историей изменения цены продажи**
- История формируется на основе данных из эндпоинта
- В таблице отображаются:
  - **Дата изменения**
  - **Старая цена**
  - **Новая цена**
  - **Комментарий** (`reason`)
- История изменения цены доступна только если `hasSalePriceHistory = true`

---

### 🔙 Backend

#### 📥 `GET /lots/{lotId}`
Возвращает данные лота

#### 📥 `GET /lots/{lotId}/members`
Список участников + инвестиции + рефералы

#### 📥 `GET /lots/{lotId}/expenses`
Список расходов

#### 📥 `GET /lots/{lotId}/financials`
Финансовая аналитика

#### 📤 `PATCH /lots/{lotId}`
Обновление параметров лота

#### 📤 `POST /lots/{lotId}/members`
Добавление участника в лот

#### 📤 `POST /lots/{lotId}/expenses`
Добавление расхода

#### 📤 `GET /lots/{lotId}/sale-price-history`
История изменения цены продажи

---

### 📦 Работа с БД

- `GET /lots/:lotId`
  - Ищет запись в `lots`
  - Включает `lot_financials`, `lot_status_history`, `lot_sale_price_history`

- `GET /lots/:id/members`
  - Достаёт из `lot_members`, `lot_investments`
  - Join с `users` и `broker_members` (для отображения имени)

- `GET /lots/:id/expenses`
  - Из `lot_expenses` + `lot_transactions`

- `POST /lots/:id/members`
  - Добавляет в `lot_members`
  - Инвестиции создаются в `lot_transactions` + `lot_investments`

- `POST /lots/:id/expenses`
  - Добавляет `lot_transaction` типа `expense` и `lot_expense`

- `GET /lots/{lotId}/sale-price-history`
  - Ищет все записи в таблице `lot_sale_price_history`, где `lot_id = :lotId`
  - Сортирует по `created_at` по убыванию

---

### 🧠 Бизнес-правила

#### 📌 Поведение в зависимости от типа сделки (`dealType`)

В зависимости от типа сделки отображаются только релевантные блоки интерфейса:

| Тип сделки (`dealType`) | Участники            | Инвестиции | Доли | Рефералы | Расходы | Доходы по участникам | Комиссия брокера                                   | Налог брокера |
|-------------------------|----------------------|------------|------|----------|---------|----------------------|----------------------------------------------------|---------------|
| `self_funded`           | ✅ Только брокер      | ✅          | ❌    | ❌        | ✅       | ✅                    | ❌                                                  | ✅             |
| `syndicated`            | ✅ Брокер + участники | ✅          | ✅    | ✅        | ✅       | ✅                    | ✅ `brokerCommissionPercent`                        | ✅             |
| `agency`                | ✅ Только брокер      | ❌          | ❌    | ❌        | ✅       | ✅                    | ✅ `brokerCommission` или `brokerCommissionPercent` | ❌             |

---

#### 🔒 Ограничения при статусе `sold` или `closed`

Если у лота установлен статус `sold` или `closed`:

- ❌ Нельзя редактировать параметры лота
- ❌ Нельзя добавлять, изменять или удалять участников
- ❌ Нельзя добавлять или изменять расходы
- ✅ Все данные доступны только для чтения
- ✅ Финансовая информация продолжает отображаться

Ограничения реализуются как на frontend (disable UI), так и на backend (валидация на уровне API).

---

### 📐 Расчётные поля

Следующие поля рассчитываются автоматически на основе связанных данных:

#### 💰 Общие значения

| Поле                        | Как рассчитывается                                                                                                            |
|-----------------------------|-------------------------------------------------------------------------------------------------------------------------------|
| Себестоимость (`totalCost`) | `purchasePrice + totalExpensesInCost`, где `totalExpensesInCost` — сумма расходов, отмеченных как «учитывать в себестоимости» |

#### 📊 Расчёты по участникам

| Поле                    | Как рассчитывается                                                               |
|-------------------------|----------------------------------------------------------------------------------|

- При включённом `autoShareByInvestment = true`, доли рассчитываются автоматически на основе инвестиций, поля при этом можно отредактировать
- После перехода в статус `sold` или `closed` расчёты фиксируются и становятся только для чтения